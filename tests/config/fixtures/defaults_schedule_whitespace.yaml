version: "1.0"
defaults:
  schedule: "   "
  storage:
    type: local
    local_path: ./backups
databases:
  whitespace-job:
    type: postgres
    host: localhost
    username: sentinel
    password_env: PG_PASSWORD
    database: app
