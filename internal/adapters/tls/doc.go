// Package tls implements ports.Prober (TLS connection probing) and provides
// TLS configuration / certificate / Mongo PEM-material helpers as
// package-level functions. See ADR 0001 (hexagonal architecture) and
// internal/ports/tls.go for the port contract.
package tls
